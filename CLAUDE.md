# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

`quantum-project` — a Python 3.13 project in early development.

## Commands

Run the project:
```
python main.py
```

Install the package locally:
```
pip install -e .
```

## Architecture

Currently a single-file Python script (`main.py`) with no external dependencies. Configuration is managed via `pyproject.toml`.
